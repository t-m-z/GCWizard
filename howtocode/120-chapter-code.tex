\chapter{The source code - Structure, common elements and coding}

\section{Structure}
\input{120-directory}

\section{Common elements}
lib widgets commons = GCWWidgets

\section{Coding}

\subsection{Structure}
Selection lists

\subsection{Layout}
column - row, expanded, child, children, container
conditions ? :

\subsection{Style guide}
\begin{description}
    \item[Indents] two spaces
    \item[Naming] lowerCamelCase; underscore for input/output
    \item[internationalization] lower case
    \item[Search strings] lower case; base search string ends with a space
\end{description}

\subsection{Continuosly computing the input}
- split the input into a list
- map every element of the list
- join the list - maybe with a delimiter

like in Morse

  if (input == null || input == '')
    return '';

  return input
         .toUpperCase()
         .split('')
         .map((character) {
            if (character == ' ')
              return '|';
    
            var morse = AZToMorse[character];
            return morse != null ? morse : '';
          })
         .join(String.fromCharCode(8195)); // using wide space


\subsection{Error handling}
return 0
return ''
return null

class IncreaseNOutput(){
final state;
final error;
final result;
}
this.state  this.error   this.result